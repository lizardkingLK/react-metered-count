export const containerStyles = (size: number) => ({
  height: size,
});

export const itemStyles = (size: number) => ({
  width: size,
  height: size,
  fontSize: size,
});

export const separatorStyles = (size: number) => ({
  fontSize: size,
});
